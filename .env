# Copy to .env (same directory as docker-compose.yml), fill real values,
# and ensure .env is in .gitignore.
P1_DB_PASSWORD=replace_with_a_real_password
P1_PGADMIN_PASSWORD=replace_with_a_real_password
